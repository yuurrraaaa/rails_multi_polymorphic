<%= form_with(model: vector_datum) do |form| %>
  <% if vector_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vector_datum.errors.count, "error") %> prohibited this vector_datum from being saved:</h2>

      <ul>
        <% vector_datum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :research_id %>
    <%= form.text_field :research_id %>
  </div>

  <div class="field">
    <%= form.label :data_id %>
    <%= form.text_field :data_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
